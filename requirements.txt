# Runtime dependencies — exact list matches imports in src/core/*.py
# starlette and uvicorn are pulled in transitively by mcp, but listed
# explicitly because core/assembly.py imports them directly for HTTP transport.
mcp[cli]
openstacksdk
uvicorn
starlette
